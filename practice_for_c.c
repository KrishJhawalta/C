//
// Created by krish on 5/11/24.
//
#include <stdio.h>
int main() {
// program for sum of n no. of natural numbers by taking user input
  /*  int n;
  int sum = 0;
  printf("Enter a number: ");
  scanf("%d", &n);
  sum = n*(n+1)/2;
  printf("The sum of the given numbers is %d", sum);

    */

  // program that takes input and prints value
  /*int a;
  printf("enter a number: ");
  scanf("%d", &a);
  for(int i = 1; i<=a; i++) {
    printf("\n%d ", i);
  }*/

// program that takes input and calculate the factorial
  // int n;
  // int factorial = 1;
  // printf("Enter your number: ");
  //   scanf("%d", &n);
  // for (int i = 1; i<=n; i++) {
  //   factorial *= i;
  // }printf("The factorial of %d is: %d",n,factorial);
  //

// program that print multiplication table vis input
  // int a;
  // printf("Enter a number: ");
  // scanf("%d", &a);
  // for(int i = 1; i<=10; i++) {
  //   printf("\n%d X %d = %d",a, i,a*i);
  // }
/*
 * program to print a number reverse
  int a, rev =0;
  printf("Enter a number: ");
  scanf("%d", &a);
  while(a != 0) {
    int digit = n%10;
    rev = rev * 10 + digit, a /=0;
  }printf("reverse number: %d\n", rev);
*/








  return 0;
  }

